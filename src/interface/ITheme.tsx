import { IColor, IHourFormat } from ".";

export interface ITheme {
    // attributes
    color : IColor;
    hourFormat: IHourFormat;
    darkMode : boolean;
    smallWindow : boolean;

    // methods
    // setters

}